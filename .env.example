FLASK_ENV=development
FLASK_DEBUG=1
# Default keeps the API private to this computer
# Use APP_HOST = 0.0.0.0 only when sharing the Flask API on the same LAN
# LAN mode refuses to start with debug enabled; use FLASK_DEBUG=0 or --no-debug
APP_HOST=127.0.0.1
APP_PORT=5001
LOG_LEVEL=INFO